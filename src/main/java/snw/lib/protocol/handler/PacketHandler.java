package snw.lib.protocol.handler;

/**
 * Represents a packet handler.
 *
 * @author SNWCreations
 * @implNote In the traditional Client/Server protocol model, there should be two packet handlers
 * which are used separately to handle server->client ("S2C") packets and
 * client->server ("C2S") packets in the application, this model should be implemented
 * by the application developers.
 * @since 1.0.0
 */
public interface PacketHandler {
}
